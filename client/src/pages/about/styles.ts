import styled, { CSSObject } from 'styled-components';

// import { colors } from '@src/constants';

interface IAboutPageStyles {
  aboutPageWrapper: CSSObject
}

const aboutPageStyles: IAboutPageStyles = {
  aboutPageWrapper: {

  },
}

export const AboutPageWrapper = styled.div`
  ${ aboutPageStyles.aboutPageWrapper }
`;
